import React, { PureComponent } from 'react';

import RoundTimerContainer from './RoundTimer.style';

class RoundTimer extends PureComponent {
  render() {
    return (
      <RoundTimerContainer>
        30!
      </RoundTimerContainer>
    );
  }
}

export default RoundTimer;
